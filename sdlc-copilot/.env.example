# --- OpenAI / AI service ---
# Leave empty to run in deterministic mock mode (still demo-able end-to-end).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
FORCE_MOCK=false

# --- Postgres ---
POSTGRES_USER=sdlc
POSTGRES_PASSWORD=sdlc
POSTGRES_DB=sdlc_copilot

# --- .NET API ---
ASPNETCORE_ENVIRONMENT=Development
